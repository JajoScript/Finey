interface Props { }

export default function IndexScreen(props: Props) {
  // 1. Manejo del estado.
  const { } = props;

  // 2. Ciclo de vida.
  // 3. Metodos.
  // 4. Render.
  return (
    <section className="w-full flex flex-col h-screen content-center justify-center">
      <div className="w-full sm:w-1/2 lg:w-1/3 m-auto flex justify-center">
        <h1>INDEX Screen</h1>
      </div>
    </section>
  )
}